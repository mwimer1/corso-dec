// Dashboard barrel (canonical)
// Keep table config surface minimal and nav canonical.

export * from './nav';

// NOTE:
// Removed previously re-exported legacy symbols due to no consumers /
// duplication with services types / legacy nav names:
// ColumnConfig, FilterConfig, FilterType,
// NAVIGATION_ITEMS, EXPORT_OPTIONS, getResultCountStyle,
// formatCellValue, TableColumnDescriptor, formatValue, ChatTableColumn,
// DashboardNavItem, DashboardNavConfigItem, NAV_CONFIG,
// GridId, GridConfig


