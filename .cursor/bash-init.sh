#!/usr/bin/env bash
# Cursor shell integration stub
# Defines dump_bash_state to prevent "command not found" errors
# This file is sourced via BASH_ENV before any profile scripts run
dump_bash_state() { :; }
export -f dump_bash_state
