================================================================================
P0 HARDENING - QUICK TEST URLs
================================================================================

IMPORTANT: You must be SIGNED IN before testing these URLs.
Open http://localhost:3000 first and sign in, then test each URL below.

================================================================================
TEST 2.1: Invalid sortBy (should return 200 OK + warning)
================================================================================
http://localhost:3000/api/v1/entity/projects?page=0&pageSize=50&sortBy=not_a_field&sortDir=desc

Expected:
- Status: 200 OK
- Response: { "data": [...], "total": N }
- Server console: Warning about invalid sortBy field
- Sort is ignored (uses default)

================================================================================
TEST 2.2: Invalid filter field (should return 200 OK, field dropped)
================================================================================
http://localhost:3000/api/v1/entity/projects?page=0&pageSize=50&filters=%5B%7B%22field%22%3A%22not_a_field%22%2C%22op%22%3A%22eq%22%2C%22value%22%3A%22x%22%7D%5D

Expected:
- Status: 200 OK
- Response: { "data": [...], "total": N } (invalid filter dropped)
- Server console: Warning about invalid filter field

================================================================================
TEST 2.3: Invalid filters format (should return 400)
================================================================================
http://localhost:3000/api/v1/entity/projects?page=0&pageSize=50&filters=%7B%22bad%22%3A%22format%22%7D

Expected:
- Status: 400 Bad Request
- Response: { "success": false, "error": { "code": "INVALID_FILTERS", ... } }

================================================================================
TEST 3: Schema Warning (Dev-only)
================================================================================
1. Temporarily edit: components/dashboard/entity/projects/config.ts
   Change line 19: defaultSortModel: [{ colId: 'fake_field_test', sort: 'desc' }]
2. Reload: http://localhost:3000/dashboard/projects
3. Check browser console → Should see ONE warning
4. Grid should still load rows
5. Revert the change

================================================================================
TEST 4: Request Storm Check
================================================================================
1. Open: http://localhost:3000/dashboard/projects
2. DevTools → Network → Filter: Fetch/XHR
3. Clear network log → Reload page
4. Count /api/v1/entity/projects requests → Should be 1-2 total

================================================================================
VERIFICATION CHECKLIST
================================================================================
[ ] Test 2.1: Invalid sortBy returns 200 + warning
[ ] Test 2.2: Invalid filter field returns 200 + warning (field dropped)
[ ] Test 2.3: Invalid filters format returns 400
[ ] Test 3: Schema warning appears once when field mismatch exists
[ ] Test 3: Grid still loads with schema warning (non-blocking)
[ ] Test 3: Temporary change reverted
[ ] Test 4: No request storm (1-2 requests max on page load)

================================================================================

