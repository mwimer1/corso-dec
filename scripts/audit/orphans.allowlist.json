{
  "description": "Allowlist for orphaned files audit - files that should never be deleted even if they appear unused. Note: Convention files (generated, config files) should be excluded via CONVENTION_FILE_PATTERNS, not allowlist. Barrel files are excluded by default unless --include-index is used.",
  "files": [
    "lib/auth/roles.ts",
    "lib/auth/server.ts",
    "lib/integrations/clickhouse/security.ts",
    "lib/integrations/env/index.ts",
    "lib/security/index.ts",
    "types/api/index.ts",
    "scripts/audit/css/report.ts"
  ],
  "notes": {
    "lib/auth/roles.ts": "assertRole function used in tests and documented in security standards as public API. TODO: Move to tests/helpers or ensure production usage if truly public API",
    "lib/auth/server.ts": "requireUserId function used in tests (deprecated but still referenced). TODO: Migrate tests to use replacement and remove this deprecated export",
    "lib/integrations/clickhouse/security.ts": "mapClickhouseError used via require() in query-utils.ts. TODO: Improve require() detection in importer map so this can be removed from allowlist",
    "lib/integrations/env/index.ts": "getEnv functions used extensively throughout codebase. TODO: Verify with import graph and remove from allowlist once detection proves usage",
    "lib/security/index.ts": "maskSensitiveData exported and used in multiple files. TODO: Verify with import graph and remove from allowlist once detection proves usage",
    "scripts/audit/css/report.ts": "CSS audit reporter module (part of css/ subsystem). Currently not imported but intended for use. TODO: Add import in orchestrator or css-audit.ts, or verify if truly unused"
  }
}

