
/** Note: Indirectly consumed via dashboard barrel; allowlisted in unused-exports to avoid FP. */

export function CorsoAIMode() {
  return <span className="text-base font-semibold">CorsoAI</span>;
}
