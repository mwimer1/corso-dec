{
  "_comment": "Tokens allowed to be unused (storybook-only, marketing-only, etc.). Names are raw CSS variable names without the leading --. Note: Many tokens appear unused because they're consumed via Tailwind classes, not direct var() calls. The audit script only detects var(--token-name) usage, not Tailwind integration.",
  "allowed": [
    "color-elevated-*",
    "text-*",
    "font-*",
    "leading-*",
    "tracking-*",
    "duration-*",
    "delay-*",
    "easing-*",
    "radius-*",
    "surface-hover",
    "input",
    "info",
    "success-foreground",
    "warning-foreground",
    "danger-foreground",
    "shadow-xs",
    "hero-*",
    "sb-*"
  ]
}

